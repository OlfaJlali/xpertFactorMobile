import { StyleSheet } from 'react-native';
import { COLOR_BLACK } from './globalStyles';

export const inputStyles = StyleSheet.create({
  input: {
    // width: '100%',
    // height: 50,
    // borderColor: '#ccc',
    // borderWidth: 1,
    // borderRadius: 5,
    // paddingHorizontal: 15,
    // marginBottom: 20,
    width: '100%',
    height: 50,
    borderColor: COLOR_BLACK,
    borderWidth: 1,
    borderRadius: 5,
    paddingHorizontal: 15,
    backgroundColor: '#FFF'
  },
  
});
