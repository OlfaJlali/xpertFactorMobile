import { StyleSheet } from 'react-native';

export const listDashboardTabsStyles = StyleSheet.create({
  tabContainer: {
    flexDirection: 'row',
    alignSelf:'center',
    gap:16,
    marginVertical: 16,
  },
});